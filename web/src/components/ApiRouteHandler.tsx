/**
 * Silent handler for /api routes
 * Prevents React Router warnings for backend API endpoints
 */
import React from 'react';

const ApiRouteHandler: React.FC = () => {
  // This component does nothing - it just prevents React Router warnings
  // The /api path is for backend API calls, not frontend routing
  return null;
};

export default ApiRouteHandler;





